{% extends 'base.html.twig' %}

{% block title %}Hello ArticleController!{% endblock %}

{% block body %}

<table class="table">
        <tbody>
            <tr>
                <td>{{ page.titlePage }}</td>
            </tr>
            <tr>
                <td>{{ page.creation }}</td>
            </tr>
            <tr>
                <td>{{ page.imagePages }}</td>
            </tr>
           {% for content in page.contents  %}
            <tr>
                <td>{{ content.titleContent }}</td>
            </tr>
            <tr>
                <td>{{ content.content }}</td>
            </tr>
            
            {# <tr>
                <td>{{ image.nameImage }}</td>
            </tr> #}
            {% else %}
            <h2> Article vide </h2>
            {% endfor %}
        </tbody>
    </table>

<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}!</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/wamp64/www/Tp_Groupe/symfony_tpgroupe/src/Controller/ArticleController.php'|file_link(0) }}">src/Controller/ArticleController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/wamp64/www/Tp_Groupe/symfony_tpgroupe/templates/article/index.html.twig'|file_link(0) }}">templates/article/index.html.twig</a></code></li>
    </ul>
</div>

{% endblock %}
