{% extends 'base.html.twig' %}

{% block title %}Hello MainController!
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	{% for page in pages %}
	<div class="container">
		<div class="displayMain1 col-lg-12">

		<div class="main1">
            <h5>{{ page.titlePage }}</h5>
            <p>{{ page.creation ? page.creation|date('Y-m-d H:i:s') : '' }}</p>
            <p>{{ page.summary }}</p>
        </div>
            
		<div class="main2">{{ page.imagePages }}</div>
            
		</div>
	</div>
    <p>teste</p>
	{% endfor %}

{% endblock %}
